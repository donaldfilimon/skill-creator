# skill-creator

This repository is an agent skill, not an application.

- Edit `SKILL.md` for triggers and hard rules.
- Edit `references/` for depth. Do not duplicate numbers outside `MLAI-master-reference.md`.
- A target stays a target until a measured result replaces it in the master reference.
- Do not expand WDBX in body copy.
- Apple language is only the public-frameworks sentence.

When updating the MLAI site from this skill, keep facts in `lib/brand.ts` and render them through provenance-tagged components.
